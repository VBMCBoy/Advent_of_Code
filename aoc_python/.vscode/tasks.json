{
    "version": "2.0.0",
    "tasks": [
    {
        "label": "Run current file",
        "type": "shell",
        "command": "python3 ${file}",
        "options": {
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "cwd": "${fileDirname}"
        }
    }
    ]
}
